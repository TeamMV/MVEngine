use std::collections::VecDeque;

/*

//world
let event = MoveEvent::new();
game.events.dispatch(event);

//example mod
handle.cancel();
handle.pause();

// after server response
handle.resume();


// game loop
game.events.process_listeners();
game.events.on_frame(|event| {
    match event {

    }
});

 */
