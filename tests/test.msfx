input num: number;

let a = 11;
let b = 3.141592653589;

a += 1.5;
b *= 2;

let d = sin[(a / 2) + 2 * (b + 3)];

let { = 0;
let ? = 0;

for i let begin[start: 50, end: 10, step: -0.5]:
    if i == 45:
        continue;
    end;
    else if i < 30:
        break;
    end;
    else if i isn't 10:
        // idk
    end;
    else:
        ? += 1;
    end;
    { += i;
end;

let i = 0;
while i < 10:
    i += 1;
end;

let 😱🚨 = sin[50];

let ماكسحمار = 10;

let vec_unnamed = vec2[1.0];
let vec_value = vec2[value: 2.0];
let vec_xy = vec2[x: 3.0, y: 4.0];
let vec_unnamed_xy= vec2[1.0, x: 3.0, y: 4.0];
let vec_value_xy = vec2[value: 1.0, x: 3.0, y: 4.0];
let vec_unnamed_x = vec2[1.0, x: 3.0];
let vec_value_y = vec2[value: 1.0, y: 4.0];

print[{, x: 😱🚨, y: ?, a: sin[20] * (1 + 3), vec: vec_xy];

let triangle = triangle2[v1: vec2[x: 0.0, y: 0.0], v2: vec2[x: 0.5, y: 1.0], v3: vec2[x: 1.0, y: 0.0]];

export rect0[x: 0, y: 0, width: 10, height: 10];
